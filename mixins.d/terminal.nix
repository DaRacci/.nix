{ config, pkgs, ... }:

{
    programs.alacritty {
        enable = true;
        settings = {
            
        };
    };

    programs.bat {
        enable = true;
        config = {
            
        };     
    };

    programs.bottom {
        enable = true;
        settings = {
            
        };
    };

    programs.broot {
        enable = true;
        settings = {
            
        };
    }
}
